# Copy to .env and fill in your keys. Never commit .env.
# LLM (for error suggestions and future harness generation)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_BASE_URL=
LLM_PROVIDER=openai

# Optional: Ollama (local)
# OLLAMA_MODEL=llama2
# LLM_PROVIDER=ollama

# Optional: CodeQL (if not on PATH)
# CODEQL_HOME=/path/to/codeql
